[project]
name = "appro"
version = "0.1.0"
description = "APPRO – supply planning cockpit & MRP simulation engine (Databricks App)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["ignore::DeprecationWarning"]
