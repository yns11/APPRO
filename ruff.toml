# Repository-level ruff configuration (backend + scripts).
line-length = 120
target-version = "py311"

[lint]
ignore = ["E741"]  # short loop variables (l for link/line) are used deliberately in comprehensions
